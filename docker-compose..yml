version: '3.8'

services:
  postgres:
    image: postgres:16.3
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_USER=123mudar
      - POSTGRES_PASSWORD=123mudar
      - POSTGRES_DB=twitterdb
